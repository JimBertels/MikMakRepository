@model MikMak2016.Models.User
@{
    //ViewBag.Title = "Login";
}

<h2>Connexion</h2>
@using (Html.BeginForm())
{
    <div class="log-in">
        <div>@Html.LabelFor(Model => Model.Login)</div>
        <hr />
        <div>@Html.LabelFor(Model=>Model.Name)</div>
        @*<div>@Html.Label("Nom d'utilisateur")</div>*@
        <div>@Html.TextBox("name") </div>
        <div>@Html.LabelFor(Model=>Model.Password)</div>
        @*<div>@Html.Label("Mot de passe")</div>*@
        @*<div>@Html.TextBox("password")</div>*@
        <div>@Html.PasswordFor(model => model.Password)</div>
        <hr />
        <input type="submit" value="Confirmer" />
    </div>
}